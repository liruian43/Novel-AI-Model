# InkOS Studio 安装与使用指南

## 🚀 快速开始

### 方式一：智能启动（推荐）⭐

**Windows 用户：**
```bash
1. 双击项目根目录下的 start.bat
   > 双击：start.bat

2. 自动检测并启动
   > ✅ 自动检测 Node.js
   > ✅ 如果没有，自动下载便携版
   > ✅ 自动启动 HTTP 服务
   > ✅ 自动打开浏览器

3. 选择项目文件夹
   > 首次使用会弹窗提示
   > 输入你的小说项目路径
   > 例如：E:\我的小说\吞天魔帝

4. 开始使用！
```

**macOS/Linux 用户：**
```bash
1. 打开终端
2. 进入项目文件夹
3. 运行：./start.sh
4. 后续步骤同上
```

---

### 方式二：手动启动

**前提条件：**
- 已安装 Node.js v20+
- 已安装项目依赖

**步骤：**
```bash
# 1. 安装依赖（仅第一次）
npm install

# 2. 启动服务
npm run studio:server

# 3. 打开浏览器访问
http://localhost:3000/InkOS-Studio.html

# 或者直接双击 HTML 文件
```

---

## 📦 核心功能说明

### 1. 智能 Node.js 检测与下载

**首次运行时：**
```
检测系统 Node.js
  ↓
未安装或版本过低？
  ↓
自动下载便携版 Node.js v20
  ↓
下载到：./node-portable/
  ↓
解压并使用
```

**以后运行时：**
```
检测系统 Node.js
  ↓
已安装且版本 OK？
  ↓
直接使用系统的 Node.js
```

**优点：**
- ✅ 用户无需手动安装 Node.js
- ✅ 不污染系统环境
- ✅ 便携版只在项目文件夹内
- ✅ 删除项目即删除，无残留

---

### 2. HTTP 服务自动启停

**打开时：**
```
双击 start.bat
  ↓
launcher.js 运行
  ↓
检测服务是否在运行（端口 3000）
  ↓
未运行 → 启动新服务
  ↓
已运行 → 跳过启动
  ↓
打开浏览器
```

**关闭时：**
```
关闭浏览器标签页
  ↓
HTML 发送 /disconnect 请求
  ↓
服务端活跃客户端数 -1
  ↓
如果客户端数 = 0
  ↓
等待 2 秒（防止误关）
  ↓
自动关闭服务进程
```

**多标签页支持：**
```
打开第 1 个标签 → 客户端数=1 → 服务运行
打开第 2 个标签 → 客户端数=2 → 服务继续
关闭第 1 个标签 → 客户端数=1 → 服务继续
关闭第 2 个标签 → 客户端数=0 → 服务关闭
```

---

### 3. 直接读写项目 MD 文件

**核心原理：**
```
HTML 界面
  ↓
HTTP 请求 (fetch)
  ↓
server.js (Node.js)
  ↓
文件系统 (fs模块)
  ↓
books/{bookId}/state/*.md
```

**数据流向：**
```
用户在 HTML 编辑真相文件
  ↓
点击"保存"
  ↓
发送 POST /write-file
  ↓
server.js 写入磁盘
  ↓
books/吞天魔帝/state/current_state.md
  ↓
✅ 真实文件被修改
```

**CLI 同步：**
```
HTML 保存后...

在终端运行：
cd books/吞天魔帝
inkos review list

✅ 看到刚才在 HTML 里修改的内容！
```

---

## 🔧 技术架构

```
┌─────────────────────────────────────┐
│         用户操作                     │
│   双击 start.bat                    │
└──────────────↑──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│     launcher.js (智能启动器)         │
│  - 检测 Node.js                      │
│  - 下载便携版（如果需要）            │
│  - 启动 server.js                    │
│  - 打开浏览器                        │
└──────────────↑──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│     server.js (HTTP 服务)            │
│  - 监听端口 3000                     │
│  - 处理文件读写请求                  │
│  - 管理客户端连接计数                │
│  - 自动关闭（无客户端时）            │
└──────────────↑──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│     InkOS-Studio.html (图形界面)    │
│  - 显示书籍列表                      │
│  - 编辑真相文件                      │
│  - 章节导出                          │
│  - 文风仿写                          │
│  - 所有操作通过 HTTP 请求             │
└──────────────↑──────────────────────┘
               │
┌──────────────┴──────────────────────┐
│     项目文件夹                       │
│  books/                             │
│  └── 吞天魔帝/                      │
│      └── state/                     │
│          ├── current_state.md       │
│          ├── particle_ledger.md     │
│          └── ...                    │
└─────────────────────────────────────┘
```

---

## 📁 文件结构

```
inkos/
├── InkOS-Studio.html          ← 图形界面（用户看到的）
├── launcher.js                ← 智能启动器（自动运行）
├── server.js                  ← HTTP 服务（后台运行）
├── start.bat                  ← Windows 启动脚本（双击这个）
├── start.sh                   ← macOS/Linux 启动脚本
├── package.json               ← 项目配置（已更新）
│
├── node-portable/             ← 便携版 Node.js（自动下载）
│   └── node-v20.11.0-win-x64/
│       └── node.exe
│
├── packages/                  ← CLI 核心代码
│   ├── core/
│   └── cli/
│
└── books/                     ← 你的小说项目
    └── 吞天魔帝/
        ├── book.json
        └── state/
            ├── current_state.md
            ├── particle_ledger.md
            └── ...
```

---

## ⚠️ 常见问题

### Q1: 为什么下载很慢？

**答**: 便携版 Node.js 从官方服务器下载（nodejs.org），国内可能较慢。

**解决方案**:
1. 耐心等待（仅需一次）
2. 使用代理
3. 手动下载安装 Node.js：https://nodejs.org/

---

### Q2: 杀毒软件报毒怎么办？

**答**: 某些杀毒软件可能会误报下载的 Node.js。

**解决方案**:
1. 添加到白名单
2. 手动从官网下载 Node.js
3. 使用方式二手动启动

---

### Q3: 关闭浏览器后服务没关闭？

**答**: 可能有以下原因：
- 还有其它标签页在使用
- 网络请求失败（/disconnect 未发送）
- 2 秒延迟还未到

**手动关闭方法**:
```bash
# Windows
任务管理器 → 找到 node.exe → 结束任务

# macOS/Linux
ps aux | grep node
kill <PID>
```

---

### Q4: 端口 3000 被占用怎么办？

**答**: 修改 `server.js` 中的端口号：

```javascript
const PORT = 3000;  // 改成其他端口，如 3001
```

然后重启服务。

---

### Q5: 如何在 CLI 和 HTML 之间切换？

**答**: 完全无缝切换！

```
上午用 HTML：
1. 双击 start.bat
2. 图形界面操作
3. 编辑真相文件
4. 关闭浏览器

下午用 CLI：
1. 打开终端
2. cd books/吞天魔帝
3. inkos review list
4. ✅ 看到上午在 HTML 里的修改！

第二天又用 HTML：
1. 双击 start.bat
2. ✅ 看到 CLI 里新增的章节！
```

**关键**: 两者操作的是同一份 MD 文件！

---

## 🎯 最佳实践

### 1. 固定项目路径

首次使用时设置好项目路径后，会保存在浏览器的 localStorage 中。

**位置**: `localStorage.getItem('inkos_project_path')`

**建议**: 勾选浏览器的"记住密码"类似功能，不要清除该网站的本地数据。

---

### 2. 定期备份

虽然数据直接存在项目文件夹，但建议：

```bash
每周执行：
git add books/
git commit -m "每周备份"
git push
```

或者手动复制整个项目文件夹到云盘。

---

### 3. 多设备同步

```
设备 A（家里）：
- 创作
- git push

设备 B（公司）：
- git pull
- 继续创作
```

因为数据是真实的 MD 文件，完美支持 Git 同步！

---

### 4. 性能优化

**大量书籍时**:
- 使用"已审核章节"筛选导出
- 避免一次性加载所有章节
- 定期清理不需要的草稿

**大文件编辑**:
- 单次编辑不超过 1MB
- 分多次保存
- 使用专业编辑器（VSCode）打开 MD 文件

---

## 🆚 版本对比

| 特性 | 旧版（LocalStorage） | 新版（HTTP 服务） |
|------|---------------------|------------------|
| **数据存储** | 浏览器缓存 | 项目 MD 文件 |
| **数据安全** | ❌ 清缓存就丢 | ✅ 永久保存 |
| **CLI 同步** | ❌ 需手动导入导出 | ✅ 自动同步 |
| **换电脑** | ❌ 数据带不走 | ✅ 整个文件夹拷贝 |
| **Git 支持** | ❌ 不支持 | ✅ 完美支持 |
| **首次启动** | ⚡ 快 | ⚡ 快（需下载 Node） |
| **日常启动** | ⚡ 快 | ⚡ 快 |
| **学习成本** | ⭐ 低 | ⭐ 低 |

---

## 📞 技术支持

遇到问题请检查：

1. **Node.js 是否安装**
   ```bash
   node --version
   # 应该显示 v20.x.x
   ```

2. **端口是否被占用**
   ```bash
   netstat -ano | findstr :3000
   ```

3. **查看错误日志**
   - 启动器输出
   - 浏览器控制台（F12）
   - server.js 的输出

**常见错误码**:
- `EADDRINUSE`: 端口被占用 → 修改端口或关闭占用进程
- `ENOENT`: 文件不存在 → 检查项目路径
- `EACCES`: 权限不足 → 以管理员身份运行

---

## 🎉 总结

**核心理念**: 
> 让不懂命令行的小白也能享受 CLI 的强大功能！

**实现方式**:
- 图形界面操作
- 后台自动管理
- 数据直接存 MD 文件
- 与 CLI 无缝同步

**用户体验**:
```
双击 start.bat
  ↓
自动打开浏览器
  ↓
图形界面点点点
  ↓
数据自动保存到磁盘
  ↓
CLI 立即可见
```

**就是这么简单！** 🚀

---

**最后更新**: 2026-03-15  
**版本**: v0.5.0-HTML-Studio
