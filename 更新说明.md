# InkOS LLM 配置更新说明

## 更新概述

本次更新为 InkOS 项目添加了**阿里云通义千问 (Qwen)** 和 **自定义大模型** 的支持，同时保留了原有的 OpenAI 和 Anthropic 支持。

---

## 🎯 新增功能

### 1. 阿里云通义千问 (Qwen) ⭐

**支持模型**:
- `qwen-plus` - 性价比高，推荐用于日常写作
- `qwen-max` - 最强性能，适合复杂场景
- `qwen-turbo` - 速度快，成本低

**配置方式**:
```bash
INKOS_LLM_PROVIDER=aliyun
INKOS_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
INKOS_LLM_API_KEY=<你的阿里云 API Key>
INKOS_LLM_MODEL=qwen-plus
```

**开通步骤**:
1. 访问 [阿里云百炼平台](https://bailian.console.aliyun.com/)
2. 注册/登录阿里云账号
3. 进入"API-KEY 管理"创建新的 API Key
4. 确保开通 Qwen 模型服务
5. 复制 API Key 到配置文件

**优势**:
- ✅ 国内可直接访问，无需代理
- ✅ 支持支付宝/微信支付
- ✅ 中文创作能力强
- ✅ 性价比高（qwen-plus: ¥0.004/K tokens 输入）

---

### 2. 自定义大模型 (Custom) 🔧

**适用场景**:
- 本地部署的开源模型 (Ollama, vLLM, LocalAI 等)
- 其他第三方 OpenAI 兼容服务 (DeepSeek, Moonshot, 智谱 AI 等)
- 企业私有化部署模型

**配置方式**:
```bash
INKOS_LLM_PROVIDER=custom
INKOS_LLM_BASE_URL=<你的自定义 API 地址>
INKOS_LLM_API_KEY=<你的 API Key>
INKOS_LLM_MODEL=<你的模型名称>
```

**常见兼容服务示例**:

| 服务 | Base URL | 模型示例 |
|------|---------|---------|
| **Ollama** | `http://localhost:11434/v1` | `qwen2.5:7b`, `llama3.1:8b` |
| **DeepSeek** | `https://api.deepseek.com/v1` | `deepseek-chat` |
| **Moonshot** | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` |
| **智谱 AI** | `https://open.bigmodel.cn/api/paas/v4` | `glm-4`, `glm-3-turbo` |

---

## 📝 修改的文件

### 核心代码

1. **`packages/core/src/llm/provider.ts`**
   - 更新 `LLMClient` 接口，添加 `aliyun` 和 `custom` provider 类型
   - 修改 `createLLMClient` 函数支持新提供商

2. **`packages/core/src/models/project.ts`**
   - 更新 `LLLMConfigSchema` 的 provider 枚举值
   - 添加 `aliyun` 到允许的提供商列表

3. **`packages/cli/src/commands/config.ts`**
   - 更新 `set-global` 命令的 provider 描述

4. **`packages/cli/src/index.ts`**
   - 添加配置向导命令的导入和注册
   - 版本号更新到 0.4.0

### 新增文件

5. **`packages/cli/src/commands/config-wizard.ts`**
   - 新增交互式配置向导命令
   - 支持图形化选择提供商和配置参数

6. **`LLM 配置指南.md`**
   - 详细的配置说明文档
   - 包含各平台的开通指南和使用建议

7. **`更新说明.md`** (本文件)
   - 更新内容总结

### 文档更新

8. **`.env.example`**
   - 更新注释，说明所有支持的提供商
   - 添加通义千问的配置示例

9. **`README.md`**
   - 更新配置部分，添加通义千问和自定义模型说明
   - 添加模型推荐表格
   - 新增专门的 LLM 提供商配置章节

10. **`InkOS-Studio.html`**
    - 更新配置页面的提供商选项
    - 添加通义千问和自定义模型的预设值
    - 更新帮助文档的常见问题

---

## 🚀 使用方法

### 方法一：使用配置向导（推荐）

```bash
inkos wizard
```

按照提示逐步选择：
1. 选择 AI 服务提供商
2. 输入 API Base URL
3. 输入 API Key
4. 选择或输入模型名称
5. 配置 Temperature 和 Max Tokens
6. 自动保存配置

### 方法二：手动配置

编辑 `.env` 文件：

```bash
# 通义千问配置示例
INKOS_LLM_PROVIDER=aliyun
INKOS_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
INKOS_LLM_API_KEY=sk-your-qwen-api-key
INKOS_LLM_MODEL=qwen-plus

# 自定义模型配置示例
INKOS_LLM_PROVIDER=custom
INKOS_LLM_BASE_URL=http://localhost:11434/v1
INKOS_LLM_API_KEY=ollama
INKOS_LLM_MODEL=qwen2.5:7b
```

### 方法三：使用 CLI 命令

```bash
# 配置通义千问
inkos config set-global \
  --provider aliyun \
  --base-url https://dashscope.aliyuncs.com/compatible-mode/v1 \
  --api-key sk-xxx \
  --model qwen-plus

# 配置自定义模型
inkos config set-global \
  --provider custom \
  --base-url http://localhost:11434/v1 \
  --api-key ollama \
  --model qwen2.5:7b
```

### 方法四：通过 HTML 界面

1. 双击打开 `InkOS-Studio.html`
2. 点击左侧导航栏的 **"⚙️ 配置管理"**
3. 在 **"LLM 配置"** 区域选择提供商
4. 填写相应配置信息
5. 点击 **"💾 保存配置"**

---

## 📊 模型对比

| 提供商 | 模型 | 价格 (每 1K tokens) | 特点 | 推荐场景 |
|--------|------|-------------------|------|---------|
| **OpenAI** | gpt-4o | $0.005 输入 / $0.015 输出 | 综合能力最强 | 高质量长篇 |
| | gpt-4-turbo | $0.01 输入 / $0.03 输出 | 性价比高 | 日常连载 |
| | gpt-3.5-turbo | $0.0005 输入 / $0.0015 输出 | 经济实惠 | 快速草稿 |
| **Anthropic** | claude-3-5-sonnet | $0.003 输入 / $0.015 输出 | 文本自然 | 文学创作 |
| | claude-3-opus | $0.015 输入 / $0.075 输出 | 上下文强 | 复杂剧情 |
| **阿里云** ⭐ | qwen-plus | ¥0.004 输入 / ¥0.012 输出 | 中文优化 | 日常写作⭐ |
| | qwen-max | ¥0.04 输入 / ¥0.12 输出 | 最强性能 | 复杂场景 |
| | qwen-turbo | ¥0.002 输入 / ¥0.006 输出 | 速度最快 | 快速生成 |
| **自定义** | 本地模型 | 电费成本 | 隐私安全 | 数据敏感 |

*注：价格为参考值，实际以官方定价为准*

---

## ✅ 验证配置

配置完成后，使用以下命令验证：

```bash
# 查看当前配置
inkos config show-global

# 测试 API 连通性
inkos doctor

# 或使用 HTML 界面的"测试连接"按钮
```

---

## 🔄 兼容性说明

### 完全兼容
- ✅ 所有现有的 CLI 命令
- ✅ HTML 应用程序的所有功能
- ✅ 多 Agent 协作管线
- ✅ 连续性审计系统
- ✅ AIGC 检测功能
- ✅ 文风仿写功能

### 注意事项
- ⚠️ 某些模型可能不支持 function calling（工具调用）
- ⚠️ 部分模型的 token 计算方式可能不同
- ⚠️ 温度参数 (Temperature) 的效果因模型而异

---

## 🛠️ 故障排查

### 问题 1: API Key 无效

**症状**: 返回 401 错误

**解决**:
1. 检查 API Key 是否正确复制
2. 确认账户余额充足
3. 验证 API Key 权限范围

### 问题 2: Base URL 无法访问

**症状**: 网络连接超时

**解决**:
1. 检查网络连接
2. 确认 Base URL 格式正确
3. 如使用代理服务，确保配置正确

### 问题 3: 模型名称错误

**症状**: 返回 404 或模型不存在错误

**解决**:
1. 核对模型名称拼写
2. 确认该模型已开通
3. 查看服务商文档确认模型可用性

### 问题 4: 响应质量不佳

**症状**: 生成内容不符合预期

**解决**:
1. 调整 Temperature 参数（建议 0.6-0.8）
2. 更换更强的模型
3. 优化创作指导的描述
4. 适当控制章节长度

---

## 📚 相关资源

- [阿里云百炼平台](https://bailian.console.aliyun.com/)
- [通义千问文档](https://help.aliyun.com/zh/dashscope/)
- [OpenAI API 文档](https://platform.openai.com/docs/)
- [Anthropic API 文档](https://docs.anthropic.com/claude/docs/)
- [Ollama 官网](https://ollama.ai/)
- [DeepSeek 开放平台](https://platform.deepseek.com/)

---

## 📅 更新日志

### v0.4.0 (2026-03-15)

**新增**:
- ✨ 支持阿里云通义千问 (Qwen) 大模型
- ✨ 支持自定义 OpenAI 兼容的大模型
- ✨ 新增交互式配置向导 (`inkos wizard`)
- ✨ 新增详细的 LLM 配置指南文档

**改进**:
- 🎨 更新 HTML 应用程序的配置界面
- 📝 更新 README 和 .env.example
- 🔧 扩展 CLI 配置命令的提供商选项

**文档**:
- 📖 新增 `LLM 配置指南.md`
- 📖 新增 `更新说明.md`
- 📖 更新 README.md 的 LLM 配置章节

---

## 💡 最佳实践

### 1. 根据场景选择模型

- **日常连载**: qwen-plus / gpt-4-turbo
- **重要章节**: qwen-max / gpt-4o / claude-3-5-sonnet
- **快速草稿**: qwen-turbo / gpt-3.5-turbo
- **本地测试**: Ollama + 开源模型

### 2. 成本优化

- 开发阶段使用低成本模型
- 生产环境使用高质量模型
- 定期监控用量和费用
- 设置预算告警

### 3. 备份配置

- 定期导出配置数据
- 保存 API Key 到安全位置
- 记录使用的模型参数

---

## 🎉 结语

通过本次更新，InkOS 现已支持主流的大语言模型提供商，用户可以根据自己的需求、预算和地理位置选择最合适的模型。

**推荐配置**:
- 🇨🇳 **国内用户**: 首选阿里云通义千问 (qwen-plus)
- 🌍 **国际用户**: OpenAI GPT-4o 或 Anthropic Claude 3.5
- 🔒 **隐私需求**: 本地部署 Ollama + 开源模型
- 💰 **成本敏感**: qwen-turbo 或 gpt-3.5-turbo

祝您创作愉快！🎊
